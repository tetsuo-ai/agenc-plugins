---
description: Read Ledger accounts and balances without signing
argument-hint: "[account-label]"
---

Follow the `ledger-status` skill and its read-only boundary. Start with
`wallet-cli session view`; use the optional account label only after confirming
that it exists in that session. Never sign, broadcast, swap, stake, encrypt, or
reset the session.
